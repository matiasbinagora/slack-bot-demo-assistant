## 1. Contract and segment planning

- [ ] 1.1 `[backend-dev]` Leer `proposal.md`, `design.md` y `specs/chronological-explain/spec.md`; conservar PR #8 como baseline separado y registrar en la PR cualquier decisión que cambie el contrato. Predecessor: `DAY-7-TASK-012`/PR #8 head `eb7286d`. OpenSpec: `chunked-chronological-video-explain`. Tests: n/a, revisión contractual. Fixtures: n/a. Validation: `openspec status --change "chunked-chronological-video-explain" --json`. Risks: deriva de alcance, modificación accidental de exportación o task 4.4.
- [ ] 1.2 `[backend-dev]` Implementar el planner determinista de intervalos con 10 segundos para videos de hasta 30 segundos y 30 segundos para videos más largos, cubriendo toda la duración validada y limitando el máximo a 10 segmentos. Predecessor: `1.1`. OpenSpec: `specs/chronological-explain/spec.md`, Requirement 1. Tests: unidades para 0/25/30/65/300 segundos, límites y ausencia de solapamientos/huecos. Fixtures: metadatos o fixtures MP4 propios. Validation: `python -m pytest` focalizado y `compileall`. Risks: redondeo, último intervalo incorrecto, más llamadas de las permitidas.
- [ ] 1.3 `[backend-dev]` Extender el pipeline temporal para extraer hasta tres frames JPEG por intervalo en rutas controladas, conservar timestamps absolutos, deduplicar intervalos cortos y no duplicar transcripts globales sin timestamps segmentables. Predecessor: `1.2`. OpenSpec: `specs/chronological-explain/spec.md`, Requirement 2. Tests: aislamiento de frames, final corto, path safety y cleanup ante FFmpeg failure. Fixtures: MP4 propios de 25/65 segundos y fixture sin audio cuando aplique. Validation: FFmpeg/FFprobe local y `python -m pytest`. Risks: evidencia cruzada entre segmentos, path traversal, retención de audio/transcript.

## 2. Independent analysis and thread rendering

- [ ] 2.1 `[backend-dev]` Construir un `AnalysisRequest` independiente por segmento, reutilizar las guardas de presupuesto y el formato multimodal de PR #8 sin Base64 en el prompt textual, sin conversación compartida ni reintentos automáticos. Predecessor: `1.3`. OpenSpec: `specs/chronological-explain/spec.md`, Requirement 3. Tests: fake analyzer que capture requests, compruebe límites, ausencia de `image_base64` textual y no mezcla de resultados. Fixtures: evidencia JPEG generada localmente y transcript no segmentable. Validation: `python -m pytest` sobre la frontera Claude mockeada. Risks: duplicación de payload, contaminación de contexto, invocación del proveedor antes de la guardia.
- [ ] 2.2 `[backend-dev]` Actualizar el orquestador para ejecutar segmentos en orden, distinguir fallos fatales de fallos locales, continuar después de timeout/presupuesto/proveedor/respuesta inválida aislada y producir un mensaje seguro cuando todos fallen. Predecessor: `2.1`. OpenSpec: `specs/chronological-explain/spec.md`, Requirements 5 y 6. Tests: fallo temprano, fallo intermedio, fallo final, todos fallan, configuración ausente y retry explícito separado. Fixtures: fake analyzer con resultados y excepciones por índice. Validation: `python -m pytest` con Slack client mockeado y `git diff --check`. Risks: falso éxito, jobs duplicados, estado de thread inconsistente.
- [ ] 2.3 `[backend-dev]` Implementar el renderer inglés agrupado por intervalos absolutos, manteniendo `channel`/`thread_ts`, timestamps solo cuando estén soportadas y notas explícitas para intervalos no disponibles; dividir únicamente en fronteras de segmento si existe un límite seguro de publicación. Predecessor: `2.2`. OpenSpec: `specs/chronological-explain/spec.md`, Requirement 4. Tests: orden, timestamps disponibles/no disponibles, gaps y salida de varios mensajes si se requiere. Fixtures: resultados estructurados fake, sin media real en assertions de texto. Validation: `python -m pytest` y revisión de redacción/redaction. Risks: salida fuera de orden, timestamp afirmada sin evidencia, mensaje Slack excesivo.

## 3. Verification, cleanup, and handoff

- [ ] 3.1 `[backend-dev]` Añadir o actualizar tests de cleanup para éxito, éxito parcial, fallo total, timeout, cancelación y fallo de publicación; verificar que no quedan source video, frames, audio, transcripts ni rutas privadas en logs. Predecessor: `2.2`. OpenSpec: `specs/chronological-explain/spec.md`, Requirement 7. Tests: assertions de workspace vacío/eliminado y logs redacted. Fixtures: workspaces temporales y errores simulados. Validation: `python -m pytest`, búsqueda de patrones de secretos y `git diff --check`. Risks: cleanup prematuro/incompleto, fuga de contenido sensible.
- [ ] 3.2 `[backend-dev]` Ejecutar validación completa y actualizar los artefactos OpenSpec de esta change en la misma PR si la implementación cambia comportamiento: `python -m pytest`, `python -m compileall -q src tests`, `openspec validate "chunked-chronological-video-explain" --json`, `openspec doctor --json`, `git diff --check` y smoke FFmpeg/FFprobe con fixtures propios. Predecessor: `3.1`. OpenSpec: `chunked-chronological-video-explain`, Requirements 1-8. Tests: suite completa y CI Python 3.10/3.14. Fixtures: videos/payloads/respuestas mockeadas del repositorio. Validation: adjuntar evidencia fresca; declarar explícitamente que no hubo Slack/Claude live QA en CI. Risks: evidencia stale, dependencia no instalada, claim incorrecto de live QA.
- [ ] 3.3 `[backend-dev]` Crear un PR separado desde `feature/task-013-chronological-explain`, enlazar PR #8, incluir resumen de alcance/non-scope, archivos modificados, comandos y resultados, riesgos, cleanup y nota de QA live no ejecutado por el worker. Predecessor: `3.2`. OpenSpec: `chunked-chronological-video-explain`. Tests: n/a, handoff/revisión. Fixtures: n/a. Validation: branch limpio salvo cambios intencionales, CI disparado y enlace de PR. Risks: merge accidental con PR #8, scope creep, falta de evidencia.
