> Regla de sincronización contractual: si cambia el comportamiento implementado, `backend-dev` debe actualizar el artefacto OpenSpec relevante (`design.md`, la `spec.md` afectada y/o `tasks.md`) en la misma PR antes de pedir review.

## 1. Contract and environment foundation

- [x] 1.1 `[backend-dev]` Leer `proposal.md`, `design.md` y las cuatro specs del change `mvp-slack-video-assistant`; documentar en la PR cualquier decisión que cambie el contrato y pedir aprobación antes de instalar dependencias, cambiar scopes Slack o tocar credenciales. Predecessor: `DAY-1-TASK-001`. OpenSpec: `mvp-slack-video-assistant`. Tests: n/a, tarea de revisión contractual. Fixtures: n/a. Validation: revisión del change y nota de alcance en la PR. Risks: deriva de alcance, cambio contractual sin rastro, cambio de credenciales/scopes sin aprobación.
- [x] 1.2 `[backend-dev]` Documentar el setup local del bot Slack, variables de entorno, proceso Python, Socket Mode, eventos y scopes mínimos (`files:read`, `chat:write` cuando apliquen), sin valores secretos. Predecessor: `1.1`. OpenSpec: `specs/slack-interaction/spec.md`. Tests: revisión documental asistida por mocks/config samples si aplica. Fixtures: n/a. Validation: revisión documental y búsqueda de secretos; no workspace live. Risks: documentación desactualizada, scopes insuficientes, exposición accidental de secretos.
- [x] 1.3 `[backend-dev]` Crear la configuración tipada y el entrypoint Python que validen variables obligatorias y arranquen/detengan Slack Bolt en Socket Mode. Predecessor: `1.2`. OpenSpec: `specs/slack-interaction/spec.md`. Tests: configuración válida/ausente y lifecycle mockeado. Fixtures: payload/config samples no secretos si ayudan al test. Validation: `python -m pytest` y comando local de arranque sin credenciales reales. Risks: fallo de startup, logs con secretos, lifecycle incompleto.

## 2. Slack interaction and thread state

- [x] 2.1 `[backend-dev]` Implementar el adaptador Slack para ack inmediato, respuestas en threads, identificación workspace/channel/thread y descarga autenticada de archivos mediante una ruta temporal controlada. Predecessor: `1.3`. OpenSpec: `specs/slack-interaction/spec.md`. Tests: payloads Bolt y cliente Slack mockeados, errores redacted, path safety. Fixtures: payloads Slack representativos sin datos reales. Validation: `python -m pytest` sobre handlers/adaptador mockeados. Risks: bloqueo del callback, descarga insegura, fuga de URLs privadas o tokens.
- [x] 2.2 `[backend-dev]` Implementar el estado efímero de sesión y los comandos canónicos `explain`, `export`, `confirm` y `cancel`, sin persistir videos ni transcripciones. Predecessor: `2.1`. OpenSpec: `specs/slack-interaction/spec.md`, `specs/video-export/spec.md`. Tests: transiciones, confirmación sin pendiente y mensajes fuera de orden. Fixtures: payloads de mensajes/thread simulados. Validation: `python -m pytest` sobre máquina de estados y handlers. Risks: estado ambiguo, comandos fuera de orden, persistencia accidental.
- [x] 2.3 `[backend-dev]` Actualizar las specs y el diseño si la implementación necesita cambiar eventos, scopes, comandos o lifecycle; registrar el cambio y su motivo en la PR antes de continuar, y hacerlo en la misma PR que introduce el cambio de comportamiento. Predecessor: `2.2`. OpenSpec: todo el change. Tests: n/a, tarea de sincronización contractual. Fixtures: n/a. Validation: `openspec validate "mvp-slack-video-assistant" --json`. Risks: divergencia entre implementación y contrato, review con requisitos obsoletos.

## 3. Claude integration

- [x] 3.1 `[backend-dev]` Implementar la configuración de `ANTHROPIC_API_KEY` y una interfaz de análisis independiente de los handlers Slack, con resultados estructurados y errores tipados. Predecessor: `1.3`. OpenSpec: `specs/claude-integration/spec.md`. Tests: clave ausente, resultado válido y respuesta inválida sin llamadas reales. Fixtures: respuestas fake/mock estructuradas, sin media sensible. Validation: `python -m pytest` sobre configuración y boundary. Risks: secreto en logs, boundary acoplada al handler, errores no tipados.
- [x] 3.2 `[backend-dev]` Integrar la implementación Claude Agent SDK detrás de la interfaz, manteniendo contenido multimedia como datos no confiables y evitando tokens, URLs privadas y transcripciones sensibles en logs o prompts de sistema. Predecessor: `3.1`. OpenSpec: `specs/claude-integration/spec.md`. Tests: cliente fake/mock, timeout y error de proveedor. Fixtures: entradas de evidencia local y respuestas mockeadas. Validation: `python -m pytest`; no requiere cuenta Anthropic real. Risks: prompt injection desde transcript, exposición de referencias Slack privadas, falso éxito ante error del proveedor.
- [x] 3.3 `[backend-dev]` Mantener `specs/claude-integration/spec.md` y `design.md` sincronizados con el SDK realmente usado; cualquier divergencia debe quedar explicada en la PR, no puede ampliarse sin aprobación y debe actualizarse en la misma PR del cambio de comportamiento. Predecessor: `3.2`. OpenSpec: todo el change. Tests: n/a, tarea de sincronización contractual. Fixtures: n/a. Validation: `openspec validate "mvp-slack-video-assistant" --json`. Risks: contrato OpenSpec obsoleto, alcance ampliado por documentación implícita.

## 4. Video understanding

- [ ] 4.1 `[backend-dev]` Implementar descarga limitada, validación MP4 con FFprobe, límites de 100 MB/300 segundos y workspace temporal privado con protección de rutas. Predecessor: `2.1`. OpenSpec: `specs/video-understanding/spec.md`. Tests: fixture válido, formato inválido, tamaño y duración excedidos. Fixtures: MP4 válidos e inválidos y metadatos controlados, todos propios del repositorio. Validation: FFprobe contra fixtures propios. Risks: bypass de límites, path traversal, consumo excesivo de recursos.
- [ ] 4.2 `[backend-dev]` Implementar extracción de frames y la frontera de audio/transcripción opcional, con degradación explícita cuando timestamps o audio no estén disponibles. Predecessor: `4.1`. OpenSpec: `specs/video-understanding/spec.md`. Tests: fixtures de video, error FFmpeg y evidencia incompleta. Fixtures: videos con y sin audio/timestamps útiles. Validation: FFmpeg/FFprobe local; no instalar Whisper sin aprobación. Risks: dependencia no aprobada, evidencia incompleta mal interpretada, fallo de extracción.
- [ ] 4.3 `[backend-dev]` Conectar la solicitud explícita `explain` con el pipeline multimedia, Claude y la respuesta en inglés del thread con resumen, key points y timestamps disponibles. Predecessor: `3.2`, `4.2`. OpenSpec: `specs/video-understanding/spec.md`, `specs/claude-integration/spec.md`. Tests: flujo end-to-end mockeado y fallos sin falso éxito. Fixtures: payloads Slack simulados y evidencia de video del repositorio. Validation: `python -m pytest` con mocks Slack/Claude y fixtures locales. Risks: resumen fabricado, respuesta fuera del thread, mezcla de evidencia no confiable con instrucciones.
- [ ] 4.4 `[backend-dev]` Implementar cleanup de videos, frames, audio, transcripciones y artefactos de análisis en éxito, error, cancelación y timeout. Predecessor: `4.3`. OpenSpec: `specs/video-understanding/spec.md`. Tests: assertions de ausencia de temporales y reporte de cleanup failure sin contenido sensible. Fixtures: directorios temporales y artefactos de prueba propios del repositorio. Validation: `python -m pytest` y verificación local de cleanup posterior a escenarios terminales. Risks: retención accidental, cleanup prematuro, fuga de contenido en errores.

## 5. Confirmed video export

- [ ] 5.1 `[backend-dev]` Implementar la sugerencia en inglés de ratios `16:9`, `9:16` o `1:1`, explicación del crop centrado y gate de confirmación/cancelación. Predecessor: `2.2`, `4.3`. OpenSpec: `specs/video-export/spec.md`. Tests: no ejecutar FFmpeg antes de `confirm`, consumo de confirmación y cancelación. Fixtures: estado de thread simulado y fixtures de video aptos para export. Validation: `python -m pytest` sobre confirmación/cancelación. Risks: confirmación ambigua, FFmpeg disparado antes de tiempo, ratios fuera de contrato.
- [ ] 5.2 `[backend-dev]` Implementar exportación FFmpeg con crop centrado, H.264/AAC y validación FFprobe de contenedor, codecs, dimensiones y duración. Predecessor: `5.1`. OpenSpec: `specs/video-export/spec.md`. Tests: fixtures landscape/portrait/square y salida inválida. Fixtures: videos propios del repositorio en las orientaciones requeridas. Validation: FFmpeg/FFprobe local. Risks: codec o contenedor incorrecto, crop no centrado, output inválido publicado.
- [ ] 5.3 `[backend-dev]` Publicar el output validado en el thread mediante Slack mockeado y ejecutar cleanup solo después de la publicación; rechazar outputs parciales. Predecessor: `5.2`. OpenSpec: `specs/slack-interaction/spec.md`, `specs/video-export/spec.md`. Tests: orden validar-publicar-borrar, error de upload y cleanup. Fixtures: salida exportada de prueba y cliente Slack mockeado. Validation: `python -m pytest` con mocks y fixtures locales. Risks: publicar antes de validar, borrar antes de subir, fuga de media ante error de upload.
- [ ] 5.4 `[backend-dev]` Mantener actualizadas las specs de exportación y el diseño si cambian ratios, confirmación, codecs, orden de publicación o política de cleanup; documentar toda divergencia en la PR y actualizar el artefacto relevante en la misma PR del cambio de comportamiento. Predecessor: `5.3`. OpenSpec: todo el change. Tests: n/a, tarea de sincronización contractual. Fixtures: n/a. Validation: `openspec validate "mvp-slack-video-assistant" --json`. Risks: contrato de exportación desactualizado, review sobre comportamiento no documentado.

## 6. QA and handoff evidence

- [ ] 6.1 `[backend-dev]` Añadir o actualizar tests, fixtures y comandos de validación para límites, seguridad, cleanup, Slack mockeado, Claude mockeado y FFmpeg/FFprobe; documentar explícitamente que no hubo Slack live QA. Predecessor: `4.4`, `5.3`. OpenSpec: todo el change. Tests: suite unitaria/integración acordada. Fixtures: videos del repositorio, payloads Slack simulados y respuestas Claude mockeadas. Validation: `python -m pytest`, checks de lint/type disponibles y validación multimedia. Risks: cobertura incompleta, falsa afirmación de live QA, fixtures insuficientes.
- [ ] 6.2 `[backend-dev]` Ejecutar `openspec validate "mvp-slack-video-assistant" --json`, revisar el diff contra el alcance aprobado y adjuntar a la PR evidencia fresca de tests, seguridad, fixtures, configuración y riesgos restantes. Predecessor: `6.1`. OpenSpec: `mvp-slack-video-assistant`. Tests: n/a, tarea de verificación final. Fixtures: reutiliza los de `6.1`. Validation: `openspec validate "mvp-slack-video-assistant" --json` y revisión del diff. Risks: evidencia stale, alcance expandido sin aprobación, cierre con validación incompleta. No mover Trello a `done` ni cambiar credenciales.
